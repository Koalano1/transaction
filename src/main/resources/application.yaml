spring:
  datasource:
    url: jdbc:mysql://localhost:6033/demo
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
app:
  write:
    enabled: true
